services:
  valgrind:
    image: valgrind
    container_name: valgrind
    build: .
    volumes:
      # - ~/.bashrc:/home/user/.bashrc
      - .:/project
    environment:
      - TERM=xterm-256color
      - USER=user
